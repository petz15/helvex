{{- if .Values.apiWorker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helvex.fullname" . }}-api-worker
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "helvex.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-worker
spec:
  replicas: {{ .Values.apiWorker.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "helvex.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api-worker
  template:
    metadata:
      labels:
        {{- include "helvex.labels" . | nindent 8 }}
        app.kubernetes.io/component: api-worker
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: api-worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: JOB_TYPE_WHITELIST
              value: "bulk,detail,initial,simap_daily,simap_backfill,shab_daily,shab_backfill,shab_archive,sogc_preprocess,extract_sogc_persons,resolve_bisher_links,repair_is_current,batch,re_geocode,recalculate_scores,recalculate_google_scores,reextract_purpose,reextract_zefix_raw,claude_classify,csv_export,analyze_boilerplate,billing_renewal,saved_view_alerts,web_extract"
            - name: JOB_WORKER_CONCURRENCY
              value: "{{ .Values.apiWorker.concurrency }}"
            {{- if .Values.mlWorker.enabled }}
            - name: ML_WORKER_INTERNAL_URL
              value: "http://{{ include "helvex.fullname" . }}-ml-worker:8000"
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ .Values.envSecretName }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
          resources:
            {{- toYaml .Values.apiWorker.resources | nindent 12 }}
{{- end }}
