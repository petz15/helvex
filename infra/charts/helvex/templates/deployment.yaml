apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helvex.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "helvex.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "helvex.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "helvex.labels" . | nindent 8 }}
        app.kubernetes.io/component: app
    spec:
      priorityClassName: helvex-web
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      automountServiceAccountToken: false
      # Job workers pause in-flight jobs at their next checkpoint on SIGTERM
      # (job_worker.request_shutdown waits up to JOB_SHUTDOWN_JOIN_TIMEOUT,
      # default 25s). The K8s default of 30s leaves no margin for that, so
      # give the drain room before the SIGKILL.
      terminationGracePeriodSeconds: 60
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            {{- if .Values.mlWorker.enabled }}
            - name: ML_WORKER_INTERNAL_URL
              value: "http://{{ include "helvex.fullname" . }}-ml-worker:8000"
            {{- end }}
            {{- if .Values.crawlerHttpWorker.enabled }}
            # Crawl worker slots across the crawler-http fleet. The web pod runs
            # no crawl jobs, but it serves the trigger route and one job row is
            # claimed by exactly one slot — so it must fan out the enqueue to
            # this many rows or the extra pods idle. Keep in step with
            # crawlerHttpWorker.replicaCount / .concurrency.
            - name: CRAWLER_HTTP_SLOTS
              value: "{{ mul .Values.crawlerHttpWorker.replicaCount .Values.crawlerHttpWorker.concurrency }}"
            {{- end }}
            {{- if and .Values.apiWorker.enabled .Values.mlWorker.enabled }}
            - name: DISABLE_JOB_WORKER
              value: "true"
            {{- else if .Values.mlWorker.enabled }}
            - name: JOB_TYPE_BLACKLIST
              value: "reclassify_noga,build_noga_embeddings,detect_language_bulk,reclassify_low_conf_noga,tfidf_kmeans_cluster,recompute_keywords,reextract_keywords,cluster_analysis,discover_stopwords,noga_v2_explain,embed_purpose_full,embed_purpose_clean,enrich_web_purpose_sim"
            {{- else if .Values.apiWorker.enabled }}
            - name: JOB_TYPE_BLACKLIST
              value: "bulk,detail,initial,simap_daily,simap_backfill,shab_daily,shab_backfill,shab_archive,link_sogc_stubs,resolve_shab_old_uids,backfill_shab_old_uid_extraction,sogc_preprocess,extract_sogc_persons,resolve_bisher_links,repair_is_current,web_search_batch,re_geocode,recalculate_scores,recalculate_google_scores,recompute_website_status,reextract_purpose,reextract_zefix_raw,claude_classify,csv_export,analyze_boilerplate,billing_renewal,saved_view_alerts,enrich_web_purpose_sim,discover_directory_domains,rescore_scope"
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ .Values.envSecretName }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 90   # up to 15 min for migrations before giving up
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {}
